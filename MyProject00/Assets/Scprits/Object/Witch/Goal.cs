using UnityEngine;

public class Witch : MonoBehaviour
{
    private float a;            //ゲームクリアシーン(まだ未作成のため仮変数で代用)


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
